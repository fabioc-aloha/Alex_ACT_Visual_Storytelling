Sales Example Verification
Date: 2026-08-06

Automated command:
	& ./tests/verify-examples.ps1

Automated result:
	PASS: all sales examples match the CSV and delivery contracts.

CSV-derived reference values:
	Revenue: $246,400
	Gross margin: $73,920 (30.0%)
	Units: 4,928
	Jan-to-Jun revenue growth: 16.6%
	Fastest Jan-to-peak segment growth: North Widget B (22.9%)

ASCII:
	Generated by templates/build-ascii-dashboard.ps1 from the CSV.
	Fresh temporary generation matches the committed output byte-for-byte.
	Every nonblank dashboard line is exactly 78 characters.
	The missing-cost schema case fails with an explicit required-column error.

HTML (verified at file:// in the VS Code integrated browser):
	ECharts 6.1.0 loaded from the exact integrity-pinned CDN URL.
	Four chart canvases rendered with zero console, page, or request errors.
	With that CDN request blocked, all four panels showed "Charts unavailable".
	Theme switching passed.
	Desktop viewport passed with no horizontal overflow.
	At the 390px mobile viewport, page width equaled client width (375px).
	All four growth-chart category labels remained visible.

SVG:
	XML, namespace, viewBox, forbidden-element, and font-size checks passed.
	Rendered at the 800px viewBox with no clipped text.
	KPI, share, and segment-growth values match the CSV contract.

Mutation testing:
	Units 4,928 -> 4,929: CAUGHT by two assertions; restored bit-identically.
	North Widget B share 22.6% -> 22.7%: CAUGHT; restored bit-identically.

Evidence boundary:
	The dataset has no campaign-spend or response fields. The examples identify
	current revenue and growth leaders but do not estimate reallocation impact.

Open limitation:
	The contract validates data, source guidance, generated ASCII, static HTML/SVG
	bindings, and rendered behavior. A clean-heir orchestrator invocation that
	regenerates all three formats remains an open audit item.
